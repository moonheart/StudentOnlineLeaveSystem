@using System.Configuration
@using Microsoft.AspNet.Identity
@using StudentOnlineLeaveSystem.Models
@{
    //User user = Session["User"] as User;
}
<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - @ConfigurationManager.AppSettings["SiteName"]</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
       <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
       <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
       <![endif]-->

</head>
<body>
    <div class="navbar navbar-default navbar-fixed-top ex-navbar">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink(@ConfigurationManager.AppSettings["SiteName"], "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("首页", "Index", "Home")</li>
                    <li>@Html.ActionLink("用户管理", "Index", "UsersAdmin")</li>
                    <li>@Html.ActionLink("角色管理", "Index", "RolesAdmin")</li>
                    @*@if (Request.IsAuthenticated)
                    {
                        if (user != null)
                        {
                            if (user.UserRoleRelations.Any(r => r.Role.RoleName == "Student"))
                            {
                                <li>@Html.ActionLink("请假", "Index", "Leave")</li>
                            }
                            if (user.UserRoleRelations.Any(r => r.Role.RoleName == "Teacher"))
                            {
                                <li>@Html.ActionLink("审批", "Index", "Checks")</li>
                            }
                        }
                    }*@
                </ul>
                @Html.Partial("_LoginPartial")
            </div>
        </div>
    </div>

    <div class="container ex-container">
        @RenderBody()
    </div>
    <div class="container hidden-xs">
        <div class="ex-footer">
            <div class="footer-copyright"> <span><a href="#">关于我们</a> · <a href="#">联系我们</a> · <a href="#">版权声明</a></span> <span class="pull-right">Copyright © 2015-@DateTime.Now.Year 在线请假系统, All Rights Reserved.</span> </div>
        </div>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
